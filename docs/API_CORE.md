# Core API

This page contains generated reference documentation for geometry, cells, buffers, layout, styles, events, and immediate interaction contracts.

```@autodocs
Modules = [
    Wicked.Core,
    Wicked.Layout,
    Wicked.Styles,
    Wicked.Events,
    Wicked.Interaction,
]
Private = false
```

