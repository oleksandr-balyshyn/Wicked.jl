# Backends and Runtime API

This page contains generated reference documentation for terminal backends, core integration, reliability, remote transport, managed runtime behavior, and clipboard boundaries.

```@autodocs
Modules = [
    Wicked.Backends,
    Wicked.CoreIntegration,
    Wicked.Reliability,
    Wicked.RemoteTransport,
    Wicked.Runtime,
    Wicked.Clipboard,
]
Private = false
```

