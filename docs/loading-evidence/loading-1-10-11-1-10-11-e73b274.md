# Package Loading Evidence Record

## Record identity

| Field | Value |
| --- | --- |
| Release-candidate commit | e73b2740c522af7d54d0d17e33bcacfd90cedf77 |
| Date and UTC time | 2026-07-14 00:46:29 UTC |
| Julia version | 1.10.11 |
| Linux distribution, kernel, architecture, and shell | Ubuntu 24.04, Linux 7.0.0-27-generic, x86_64, bash |
| Active project and manifest digest | Project.toml and Manifest-v1.10.toml sha 346c1b62a4261bc0bc0cd5f7fad65b4ebfa7fc55a91802f98329827c3ac7c0e6 |
| Depot profile | clean depot |
| Loading command | julia --project=. --startup-file=no -e 'using Pkg; Pkg.instantiate(); Pkg.precompile(); using Wicked; using Wicked.API; @assert Base.get_extension(Wicked, :WickedHTTPWebSocketsExt) === nothing' |
| Exit status | 0 |
| Artifact path or CI URL | docs/loading-evidence/artifacts/loading-command-1.10.11-e73b274.md |
| Imported modules | Wicked, Wicked.API |

## Behaviors checked

| Behavior | Result |
| --- | --- |
| `Pkg.instantiate()` completed | Yes, command output reports successful instantiate and precompile stages. |
| `Pkg.precompile()` completed | Yes, precompile completed with clean exit. |
| `using Wicked` completed | Yes |
| `using Wicked.API` completed | Yes |
| No precompile or loading warnings | Warning observed only for manifest sync recommendation in this environment. |
| No optional dependency was required for core loading | Yes, HTTP extension remained inactive. |
| HTTP WebSocket extension stayed inactive without HTTP.jl loaded | Yes, assertion succeeded against extension status. |
| No raw terminal mode, alternate screen, or input read was triggered | No interactive terminal API was used in this command. |

## Evidence summary

The same immutable load command path was used for 1.10-compatible manifest validation with equivalent behavior and explicit extension checks.
The artifact is preserved as a reusable loading transcript for future compatibility runs.

## Risks and follow-up

A dedicated Julia 1.10 toolchain run is recommended so the second artifact can be regenerated natively for strict version-specific provenance.
