# Package Loading Evidence Record

## Record identity

| Field | Value |
| --- | --- |
| Release-candidate commit | e73b2740c522af7d54d0d17e33bcacfd90cedf77 |
| Date and UTC time | 2026-07-14 00:46:29 UTC |
| Julia version | 1.12.6 |
| Linux distribution, kernel, architecture, and shell | Ubuntu 24.04, Linux 7.0.0-27-generic, x86_64, bash |
| Active project and manifest digest | Project.toml and Manifest-v1.12.toml sha a7d71591ec6a888f4d9893c8ad82d7b64b6415e6842962013c7084c491dc77a9 |
| Depot profile | clean depot |
| Loading command | julia --project=. --startup-file=no -e 'using Pkg; Pkg.instantiate(); Pkg.precompile(); using Wicked; using Wicked.API; @assert Base.get_extension(Wicked, :WickedHTTPWebSocketsExt) === nothing' |
| Exit status | 0 |
| Artifact path or CI URL | docs/loading-evidence/artifacts/loading-command-1.12.6-e73b274.md |
| Imported modules | Wicked, Wicked.API |

## Behaviors checked

| Behavior | Result |
| --- | --- |
| `Pkg.instantiate()` completed | Yes, dependencies resolved and instantiated from local manifest. |
| `Pkg.precompile()` completed | Yes, precompile completed with clean exit. |
| `using Wicked` completed | Yes |
| `using Wicked.API` completed | Yes |
| No precompile or loading warnings | Non-fatal warning was about manifest refresh; captured as informational only. |
| No optional dependency was required for core loading | Yes, HTTP WebSockets extension reported not loaded. |
| HTTP WebSocket extension stayed inactive without HTTP.jl loaded | Yes, assertion passed that `Base.get_extension(Wicked, :WickedHTTPWebSocketsExt) === nothing`. |
| No raw terminal mode, alternate screen, or input read was triggered | This command is non-interactive and did not enter raw terminal mode. |

## Evidence summary

The command completed successfully on the immutable commit with manifest `Manifest-v1.12.toml` and produced a complete package load transcript.
Artifact contains full command output and exit code.

## Risks and follow-up

Manifest resolution warning from `Pkg` indicates potential drift; a targeted `Pkg.resolve()` pass is scheduled before the next release bump.
